<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.winvents.fiap.checkpoint">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/page1"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WinventsCheckpoint">
        <activity android:name="ListaActivity" android:parentActivityName=".MainActivity" android:label="@string/page2"/>
        <activity android:name="VisualizarActivity" />
        <activity android:name="PageCursos" />
        <activity
            android:name="MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="CadastroActivity" android:label="@string/page3" />
    </application>

</manifest>
